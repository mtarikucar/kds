# Deep fake-working sweep #2 — 2026-06-24

Workflow wf_972c4d51-6bb · 52 agents · verdict FOUND_22 · counts {"round1Candidates":22,"round2Candidates":9,"confirmed":22,"rejectedOrKnown":9}

Confirmed by adversarial verification (reachable in prod, not already-known/gated). 0 blockers, 14 major, 8 minor.

## 1. [major] Public reservation booking path is NOT plan-gated — customers can book into a void on plans without reservationSystem

- **cluster:** reservations
- **file:** `backend/src/modules/reservations/controllers/public-reservations.controller.ts:29-104`
- **symptom:** A customer on a restaurant's public booking link (/reserve/:tenantId) for a tenant whose plan does not include reservations completes the wizard and sees a confirmation number, but the restaurant cannot see or act on the booking (admin Reservations page returns 403), so the guest is never seated/confirmed.
- **detail:** The authenticated ReservationsController is gated with class-level @UseGuards(...PlanFeatureGuard) + @RequiresFeature(PlanFeature.RESERVATION_SYSTEM) (reservations.controller.ts:39-40), so a tenant on STARTER/PROFESSIONAL (subscription-plans.const.ts:95,128 → reservationSystem:false) gets 403 on the admin reservations page. But the PublicReservationsController is entirely @Public() with NO PlanFeatureGuard / @RequiresFeature. PlanFeatureGuard.canActivate returns true immediately for any @Public() route (plan-feature.guard.ts:43-49), and the only global APP_GUARD is a throttler (app.module.ts:180-184), so nothing enforces the plan on the public endpoints. createPublicReservation gates ONLY on settings.isEnabled (reservations.service.ts:129), and ReservationSettings.isEnabled defaults to true (schema.prisma) with no coupling to the plan feature anywhere (grep for isEnabled vs plan/feature returns nothing). Net: a tenant whose plan excludes reservations still serves GET /public/reservations/:tenantId/settings → {isEnabled:true}, accepts POST /public/reservations/:tenantId bookings, issues a reservation number, fires the customer 'created' notification — yet the operator can NEVER view/confirm/manage them (admin page is 403). Customers book successfully into a black hole.
- **suggested fix:** Enforce the reservationSystem entitlement on the public path, server-side, so the public surface matches the admin gate. Cleanest: in ReservationSettingsService.getPublicSettings and ReservationsService.createPublicReservation (and ideally the availability reads), after validateTenant, resolve the tenant's entitlements via EntitlementService.getForTenant(tenantId) and treat reservations as disabled when feature.reservationSystem is not granted — e.g. getPublicSettings returns {isEnabled:false,...} (so the FE wizard shows 'reservations unavailable' instead of accepting a booking), and createPublicReservation throws ForbiddenException/BadRequest('Reservation system is not enabled') before persisting. Inject EntitlementService into the reservations services (it is already the canonical resolver used by PlanFeatureGuard, honoring plan + add-on grants + overrides, with the plan-only fallback for the projector race). Add an e2e/HTTP-level spec asserting a BASIC tenant gets isEnabled:false from GET /public/reservations/:tenantId/settings and a 4xx from POST /public/reservations/:tenantId, mirroring the existing branch-scope/real-DB e2e gate, since mocked-Prisma unit tests would miss the guard-bypass-on-@Public() class. Do NOT rely on flipping ReservationSettings.isEnabled, since its schema default is true and no projector writes it.

## 2. [major] getAvailableSlots accepts guestCount but ignores it — slots shown 'available' even when no table can seat the party

- **cluster:** reservations
- **file:** `backend/src/modules/reservations/services/reservation-availability.service.ts:111-216`
- **symptom:** On the public booking wizard, every time slot appears bookable for any party size; a guest can confirm a reservation for e.g. 50 people at a venue whose biggest table seats 8, with no table assigned, and receive a confirmation number.
- **detail:** getAvailableSlots(tenantId, date, guestCount?, branchId?) takes guestCount (threaded from the public controller at public-reservations.controller.ts:49-66 and from the wizard via useAvailableSlots in PublicReservationContainer.tsx:84-89), but the slot-availability loop (lines 183-213) only checks minAdvanceBooking and maxReservationsPerSlot — guestCount is never read. maxReservationsPerSlot defaults to NULL (schema.prisma, no @default), so out of the box EVERY slot returns available:true regardless of capacity or existing bookings. Step 3 (table pick) IS capacity-filtered via getAvailableTables, but Step 3 is optional (PublicReservationContainer.tsx:138 stepFields[3]=[]), so a guest can select a slot, skip the table, and submit a no-table walk-in booking for a party larger than any table the venue has. create only validates capacity against an explicitly-assigned table (reservations.service.ts:224), not against guestCount when tableId is null.
- **suggested fix:** Make guestCount actually constrain slot availability, and close the no-table capacity hole on create.

(1) In getAvailableSlots, when guestCount is provided, mark a slot available:false unless at least one table in the resolved branch can seat the party AND is free for that slot's window. Practically: load the branch's tables once (like getAvailableTables does), and per slot compute whether any table with capacity >= guestCount has no overlapping PENDING/CONFIRMED/SEATED reservation for [slotStart, slotStart+defaultDuration). This reuses the existing overlap logic and the timeToMinutes helper. Also fall back to a sane maxReservationsPerSlot semantic (or document that NULL means unlimited) so capacity isn't silently unbounded.

(2) In createPublicReservation, validate guestCount against real capacity even when dto.tableId is null. Minimal guard: after resolving the branch, fetch the max table capacity for that branch (SELECT max(capacity) FROM table WHERE tenantId=? AND branchId=?) and throw BadRequestException if dto.guestCount exceeds it (no single table can seat the party). This stops a no-table booking for a party larger than any physical table. (If multi-table seating is intended, instead require that the guestCount can be accommodated by the branch's largest table or an explicit multi-table policy — but at minimum reject parties no table can seat.)

Add a unit/e2e test: settings with maxReservationsPerSlot=NULL and a branch whose largest table seats 8 — assert getAvailableSlots(guestCount=20) returns no available slots, and POST /public/reservations with guestCount=20 and no tableId returns 400.

## 3. [major] QR-menu referral 'Apply Code' form POSTs to a route that does not exist (404 every time)

- **cluster:** loyalty-customers
- **file:** `frontend/src/components/qr-menu/LoyaltyContent.tsx:57-60`
- **symptom:** A customer on the QR-menu Rewards page types a friend's referral code, taps 'Apply', and always gets the generic error toast (t('loyalty.referralError')) — the referral is NEVER recorded and neither party gets the 100/50 bonus points. The feature looks fully built (card, input, button, success toast wired) but can never succeed.
- **detail:** This is the only referral-apply entry point in the customer UI (rendered by both SubdomainLoyaltyPage at /loyalty and LoyaltyPage at /qr-menu/:tenantId/loyalty, both mounted in App.tsx:170,199 and reachable via the QRMenuNavigation 'Rewards' tab). Fix: POST customer-public/referral/apply with { sessionId, referralCode } (the session-based contract the backend enforces; note backend also requires phoneVerified, which the UI doesn't surface).
- **suggested fix:** In frontend/src/components/qr-menu/LoyaltyContent.tsx handleApplyReferralCode: drop the session GET + customerId lookup (the backend resolves customerId from the session itself) and POST to the real endpoint with the session-based contract. Replace lines ~48-60 with:

  const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';
  if (!sessionId) { toast.error(t('loyalty.mustIdentify')); return; }
  await axios.post(`${API_URL}/customer-public/referral/apply`, {
    sessionId,
    referralCode: referralCode.toUpperCase(),
  });

Notes: (1) sessionId is already a prop on the component, so the extra session fetch is unnecessary. (2) The backend additionally enforces phoneVerified before applying (per @ApiOperation "phone must be verified"); the UI should surface that requirement / handle that error so customers know to verify their phone first (otherwise they'll still hit the generic error toast even after the path fix). (3) Add a frontend regression test asserting the POST targets `customer-public/referral/apply` with `{ sessionId, referralCode }`, since the existing LoyaltyPage/SubdomainLoyaltyPage tests mock out LoyaltyContent and never exercised this call.

## 4. [major] Loyalty redemption (redeemPoints) has zero production callers — 'Redeemable Amount / You can redeem your points!' is a dead-end

- **cluster:** loyalty-customers
- **file:** `backend/src/modules/customers/loyalty.service.ts:240-265`
- **symptom:** The customer loyalty card prominently shows 'Redeemable Amount: $X.XX' and 'You can redeem your points!' once they cross minRedeemPoints (LoyaltyDisplay.tsx:149-167), implying they can spend points for a discount. But there is no redeem button anywhere and no checkout path that consumes points, so a customer can NEVER actually redeem — points only ever accumulate.
- **detail:** Either redemption was never finished or the UI over-promises. The honest options are to wire a redeem action into the QR self-pay/checkout flow, or to stop advertising 'You can redeem your points!' (LoyaltyDisplay.tsx:158-166, i18n key loyalty.canRedeem) until redemption exists. Secondary cosmetic bug in the same card: redeemableAmount is rendered with a hardcoded '$' (LoyaltyDisplay.tsx:155) in a TRY-only system, so even the displayed redeemable figure is mis-currencied.
- **suggested fix:** Pick one of two honest paths:

(A) Wire redemption (preferred if the feature is intended). Add a guest-facing route, e.g. POST /customer-public/loyalty/redeem on CustomerPublicController, resolving customerId/tenantId from the server-side session (mirror existing handlers), validating pointsToRedeem >= minRedeemPoints and a self-pay-enabled session, and applying the resulting discountAmount to the active self-pay intent (extend PayIntentRequest in customerPayApi.ts with a points/redeem field and have the self-pay/checkout flow consume redeemPoints, decrementing the payable amount). redeemPoints already does the safe Serializable deduction with the conditional updateMany race guard, so the service layer is ready; only the controller + checkout wiring is missing. Add a Redeem button in LoyaltyDisplay.tsx gated on canRedeem.

(B) If redemption is not being shipped now, stop advertising it: remove/neutralize the loyalty.canRedeem "You can redeem your points!" copy (LoyaltyDisplay.tsx:158-161) and either hide the "Redeemable Amount" block or relabel it (e.g. "points value, redeemable in-store") so the card no longer implies a self-serve redeem the system cannot honor.

Independent of A/B, fix the currency bug: replace the hardcoded `$` at LoyaltyDisplay.tsx:155 with the project currency formatter (TRY/₺) used elsewhere, so the redeemable figure isn't mis-currencied in a TRY-only system.

## 5. [major] Order-driven low-stock realtime alert emits a dead event name (stock:low-stock-alert) that NO client subscribes to — kitchen/POS never see it, including the CRITICAL stock-reversal-failed inventory warning

- **cluster:** realtime-sockets
- **file:** `backend/src/modules/kds/kds.gateway.ts:908-916`
- **symptom:** When a real order depletes an ingredient below its minimum (the exact live moment an operator expects a 'low stock' warning), no toast ever appears on the Kitchen Display or POS. Worse, the CRITICAL path at orders.service.ts:1114 — emitted when stock REVERSAL FAILS on a cancelled order, with message 'Stock reversal failed for order ... Please verify inventory.' — also silently vanishes. The operator believes the realtime stock-alert feature works (the i18n keys kitchen:kitchen.lowStockAlert / pos:notifications.lowStockAlert exist and are wired to a toast), but the order-driven alert never reaches any screen.
- **detail:** Root cause is an event-name divergence introduced when the StockAlertsService scheduled path was remediated (memory: 'stock low/expiry alerts → KDS/POS sockets'). That remediation added emitStockLowAlert/emitStockExpiryAlert → 'stock:low-alert'/'stock:expiry-alert' and wired the FE listeners to those names, but the older order-driven emitLowStockAlert helper (emitting the legacy 'stock:low-stock-alert' name with an items[] payload) was left orphaned with all five of its callers intact. This is NOT the already-remediated scheduled-alert path; it is a distinct, still-live realtime path. Fix: either rename the emit at kds.gateway.ts:912 to 'stock:low-alert' and reshape its payload to { count, items } to match the FE handlers, or repoint the five callers at emitStockLowAlert. Verifier can confirm by grepping for any 'stock:low-stock-alert' subscriber (there is none).
- **suggested fix:** Repoint the order-driven helper at the working event name AND fix its payload shape so it matches the FE handlers (which read event.count). In kds.gateway.ts:908-916, change emitLowStockAlert to emit `stock:low-alert` with a `{ count, items }` payload:

  emitLowStockAlert(tenantId: string, branchId: string, items: string[]) {
    this.server
      .to(`kitchen-${tenantId}-${branchId}`)
      .to(`pos-${tenantId}-${branchId}`)
      .emit("stock:low-alert", { count: items.length, items, timestamp: new Date() });
    this.logger.debug(`stock:low-alert ${items.length} items → kitchen/pos-${tenantId}`);
  }

This is the lowest-risk fix: it reuses the already-subscribed name, keeps all five existing callers untouched (they still pass string[]), and the FE toast renders the correct count. Verify by re-running the existing socket specs (useKitchenSocket.test.tsx / usePosSocket.test.tsx already assert handlers for stock:low-alert) and add a backend spec asserting emitLowStockAlert emits 'stock:low-alert' with count = items.length. Optionally add a dedicated event/toast for the stock-reversal-failed CRITICAL case so it is visually distinguishable from routine low-stock, since today it shares the generic low-stock toast text once routed correctly.

## 6. [major] Loyalty 'Redeemable' / 'You can redeem!' shown to customer, but points can NEVER be redeemed (no endpoint, no caller, no UI action)

- **cluster:** selfpay-qrmenu
- **file:** `frontend/src/components/qr-menu/LoyaltyDisplay.tsx:152-166`
- **symptom:** On the QR-menu Loyalty tab, an identified customer sees a points card showing 'Redeemable: $X.XX' and a prominent banner 'You can redeem!' (loyalty.canRedeem) once they cross the 100-point minimum. There is no redeem button anywhere in LoyaltyDisplay/LoyaltyContent, no redeem API call in the frontend, and no order/checkout step that spends points. The customer is told they can redeem their loyalty value but there is literally no way to do it — the points only ever accumulate. (Bonus: the redeemable value is rendered with a hardcoded '$' at LoyaltyDisplay.tsx:155 even though the platform is TRY-only, so it also shows the wrong currency symbol.)
- **detail:** This is a genuine 'looks-working-but-isnt' for the customer: the loyalty UI advertises a redeemable balance and explicitly tells the user 'You can redeem!', but redemption is unreachable in production. redeemPoints() is fully implemented (atomic point deduction via awardPoints with REDEEMED type, discount computed at currencyPerPoint) yet has zero HTTP exposure and zero callers across the entire backend, and the frontend has no UI control or API binding to trigger it. The only customer loyalty actions wired up are: earn (automatic, post-payment via payment-finalizer earnPointsFromOrder), apply-referral-code, copy-referral-code, view transaction history, and welcome-bonus. Recommendation: either (a) wire redeemPoints into the self-pay / payByItems flow (and add a controller route + redeem control in SelfPayModal/cart so points reduce the amount charged), or (b) stop advertising redeemability — remove the 'Redeemable'/'canRedeem' UI until the redeem path ships. Also fix the hardcoded '$' at LoyaltyDisplay.tsx:155 to use formatCurrency(...) like the rest of the QR menu (TRY-only per the currency-stabilization work).
- **suggested fix:** Pick one of two directions.

Preferred — stop advertising redeemability until the redeem path ships (smallest, honest): in frontend/src/components/qr-menu/LoyaltyDisplay.tsx, remove or gate the redeemable-amount block and the canRedeem banner (lines 149-167) behind a real feature flag, OR replace the 'You can redeem!' copy with informational text that does not promise an action. Also fix the hardcoded '$' at line 155 to use the shared formatCurrency(...) helper (TRY-only) instead of the literal '$' prefix.

Alternatively — actually ship redemption: (a) add a controller route (e.g. POST /customer-public/loyalty/redeem on customer-public.controller.ts, resolving customerId/tenantId from the session like getLoyaltyBalance does) that calls loyaltyService.redeemPoints; (b) wire redeemPoints into the self-pay / payByItems flow so the computed discountAmount reduces the amount charged (and is captured atomically with the payment / order finalization, mirroring how earnPointsFromOrder is invoked post-commit in payment-finalizer.service.ts:332); (c) add a redeem control in the QR-menu cart/SelfPayModal that calls the new endpoint; (d) fix the line 155 currency symbol regardless.

## 7. [major] Superadmin "Extend" on a PAST_DUE/EXPIRED/TRIAL_ENDED subscription reports success but never unlocks the tenant (status untouched, no entitlement reprojection)

- **cluster:** subscriptions-billing
- **file:** `backend/src/modules/superadmin/services/superadmin-subscriptions.service.ts:467-530`
- **symptom:** In Superadmin → Subscriptions, every row shows an "Extend" button regardless of status (SubscriptionsPage.tsx:179-185 renders Extend for all rows; only Cancel is gated to ACTIVE at :186). findAllSubscriptions (superadmin-subscriptions.service.ts:260-292) lists ALL statuses by default, so EXPIRED / TRIAL_ENDED / PAST_DUE subscriptions are all listed with a live Extend button. When an operator clicks Extend on such a row they get a green success toast (SubscriptionsPage.tsx:53 'Abonelik uzatıldı.'), but extendSubscription only writes currentPeriodEnd (lines 496-509) — it never changes status and never emits a lifecycle/reprojection event. Both gates treat only ACTIVE/TRIALING/PAST_DUE as live (subscription-status.guard.ts:74-88, plan-feature.guard.ts:111-126), so an EXPIRED or TRIAL_ENDED tenant stays fully locked out of the app (403 PLAN_SELECTION_REQUIRED) despite the operator believing they just granted the tenant more time. For a PAST_DUE row, Extend pushes currentPeriodEnd into the future, which permanently removes it from the past-due-subscriptions expiry cron's `currentPeriodEnd <= sevenDaysAgo` window (subscription-scheduler.service.ts:336-339) — the tenant is stuck in PAST_DUE with the red dunning banner showing indefinitely.
- **detail:** Reachable in production by any superadmin/support operator. Two distinct failure modes: (1) EXPIRED/TRIAL_ENDED row → Extend succeeds (toast) but tenant remains hard-locked (guards still 403) because status was never moved to ACTIVE and no reproject event fired; the support agent thinks the customer was reinstated. (2) PAST_DUE row → Extend moves currentPeriodEnd forward, which is honored by the guards (PAST_DUE is live) but also takes the row out of the 7-day grace-expiry cron window forever, leaving the tenant permanently PAST_DUE with the dunning 'pay now' banner. Fix options: extendSubscription should also set status=ACTIVE (and clear endedAt) for non-ACTIVE rows and emit emitSubscriptionReprojection inside the same txn (mirroring updateSubscription:433-444 and the tenant-side activation paths), or the SubscriptionsPage should only render Extend for ACTIVE/PAST_DUE rows. NOTE on scope: this lives in the superadmin module, which is adjacent to (operates directly on) the subscriptions lifecycle but is not strictly the subscriptions/payments source dirs; flagging because it is an operator-facing subscription-state action that no-ops on the exact statuses (EXPIRED/TRIAL_ENDED) an operator would most want to extend.
- **suggested fix:** In extendSubscription, run inside a $transaction: when subscription.status is not ACTIVE (EXPIRED/TRIAL_ENDED/PAST_DUE/CANCELLED), also set status='ACTIVE' and clear endedAt (and cancelAtPeriodEnd/cancelledAt if set) alongside currentPeriodEnd, then call this.subscriptionService.emitSubscriptionReprojection({ id, tenantId, plan, currentPeriodStart, currentPeriodEnd: newEndDate }, tx) so the entitlement projector re-grants access — mirroring updateSubscription:433-444 and the tenant-side activation/bank-transfer-confirm paths. If reinstatement-by-extend is intentionally out of scope, instead gate the Extend button in SubscriptionsPage.tsx to only render for ACTIVE/PAST_DUE rows and surface a separate explicit Reactivate action for EXPIRED/TRIAL_ENDED that hits a reprojecting endpoint. Add an e2e test that extends an EXPIRED subscription and asserts a subsequent tenant-scoped request returns 200 (not 403 PLAN_SELECTION_REQUIRED).

## 8. [major] Local-bridge agent's 'heartbeat' only pings GET /healthz and never calls POST /v1/bridges/heartbeat → every provisioned bridge shows perpetually offline while running fine

- **cluster:** device-mesh-bridge
- **file:** `apps/local-bridge-agent/src/telemetry.rs:8-17`
- **symptom:** In the admin Bridges page (BridgesPage.tsx StatusPill) a healthy, command-processing bridge appears 'offline' with lastSeen '—' (or frozen at provision time) within ~60-120 seconds of coming up, and stays offline forever. Operators will believe their HummyBox is dead/disconnected.
- **detail:** This contradicts the device.service comment that 'the only real path to a false offline is a sustained network drop' — here a perfectly connected, working bridge is falsely offline by construction. The KDS-kiosk app gets this right (apps/kds-kiosk/src/screens/KitchenScreen.tsx heartbeat every 20s → POST /v1/devices/heartbeat), proving the intended contract; only the Rust bridge agent skips it. Fix: add a CloudTransport.post_heartbeat hitting POST /v1/bridges/heartbeat with the Bridge token + metrics, and call it from spawn_heartbeat instead of warm_up.
- **suggested fix:** Add a real heartbeat to the transport seam and call it from the loop instead of warm_up:
1) In cloud_ws.rs add to the `CloudTransport` trait: `async fn post_heartbeat(&self, metrics: &HeartbeatMetrics) -> Result<()>;` and implement it on ReqwestTransport as `POST {cloud_url}/v1/bridges/heartbeat` with `Authorization: Bridge {resolve_bearer_token()}` and a JSON body { hostname, os, agentVersion, queueDepth, lastCommandStatus, driverHealth } (the BridgeHeartbeatDto the backend already accepts — controller :98 / dto/local-bridge.dto.ts). Add a `CloudClient::post_heartbeat` wrapper and a FakeTransport impl for the existing tests.
2) In telemetry.rs:8-17 replace `let _ = cloud.warm_up().await;` with `let _ = cloud.post_heartbeat(&metrics).await;` (keep it best-effort / non-fatal as the comment intends). warm_up can stay as the one-shot boot reachability probe in main.rs:106 but must NOT be what keeps the bridge alive.
3) Confirm backend lastSeenAt grace (60s, HEARTBEAT_GRACE_MS) comfortably exceeds the 20s cadence (it does) and add an agent integration test asserting post_heartbeat is invoked on the heartbeat tick. Optionally fix the now-accurate doc-comments/README rather than leaving them aspirational.

## 9. [major] BridgeConfig.provisioning_token is parsed/stored but never read by any runtime path; the agent has no claim flow

- **cluster:** device-mesh-bridge
- **file:** `apps/local-bridge-agent/src/config.rs:24-29, 45-49`
- **symptom:** An operator who follows the documented one-shot provisioning (put the provisioning_token shown once at order fulfilment into bridge.toml) gets no effect: the agent never exchanges it for a bearer token via POST /v1/bridges/claim, so unless a bearer token is independently present in HUMMY_BRIDGE_TOKEN/keyring the agent's authenticated calls send an empty token and silently get rejected.
- **detail:** The backend side of provisioning is fully implemented (LocalBridgeService.createSlot issues a provisioningToken, claim() exchanges it for a bearer — local-bridge.service.ts:54-154), so the gap is purely the agent never consuming the stored provisioning_token. Combined with finding #1/#2 this means the documented bridge onboarding does not actually wire the bridge up. Note this is distinct from the (out-of-scope) leaked-secrets item — this is a config field with no runtime reader.
- **suggested fix:** Wire a first-boot claim flow in the agent. In main.rs, after config::load and before CloudClient warm-up/loop, add a bootstrap step: if resolve_bearer_token() is None AND cfg.provisioning_token is Some, call a new ReqwestTransport/CloudClient method claim(provisioning_token, hostname, os, agentVersion) that POSTs to {cloud_url}/v1/bridges/claim and parses { bridgeId, token, tokenExpiresAt }. Persist the returned bearer to the OS keyring (the keyring crate behind the feature flag config.rs already TODOs) so resolve_bearer_token() finds it on subsequent boots, and stop relying on unwrap_or_default() for an empty bearer — fail fast (anyhow::bail!) with a clear 'bridge not provisioned: set provisioning_token in bridge.toml or HUMMY_BRIDGE_TOKEN' message if neither a bearer nor a provisioning token is available, instead of silently sending 'Authorization: Bridge ' and getting 401s. After a successful claim, clear/ignore provisioning_token (it is single-use server-side: claim() sets provisioningTokenHash=null). Add a unit test over the CloudTransport seam (mirroring cloud_ws.rs FakeTransport) asserting claim() is invoked exactly once on first boot and the resulting bearer is used on the next get_next_commands. Also reconcile README.md (the claim step and the WSS step) with whatever is actually implemented.

## 10. [major] Superadmin plan/override UI silently can't manage deliveryIntegration, externalDisplay, posAccess (and maxBranches per-tenant) — advertised, revenue-gating plan features are unsettable through the admin panel

- **cluster:** admin-superadmin
- **file:** `frontend/src/pages/superadmin/PlansPage.tsx; frontend/src/pages/superadmin/TenantDetailPage.tsx; frontend/src/pages/superadmin/tenantOverrides.helpers.ts:PlansPage.tsx:294-308 (formData omits externalDisplay/deliveryIntegration/posAccess) + :477-568 (no checkbox for them); TenantDetailPage.tsx:36-46 FEATURE_LABELS (omits externalDisplay/deliveryIntegration/posAccess) + :48-54 LIMIT_LABELS (omits maxBranches); tenantOverrides.helpers.ts:17-27 buildFeatureOverridesPayload only iterates the keys present in featureStates`
- **symptom:** Superadmin/support opens the Plans form or the per-tenant Feature Overrides editor to enable delivery integration / partner display / POS for a customer, sees no toggle for them, saves, and gets a success toast — but the advertised, revenue-gating feature is never sent and the customer stays un-provisioned; only a direct DB edit can set it.
- **detail:** The backend fully supports these flags: superadmin-subscriptions.service.ts createPlan/updatePlan persist externalDisplay + deliveryIntegration; superadmin-tenants.service.ts FEATURE_KEYS (lines 33-46) whitelists externalDisplay, deliveryIntegration, posAccess and LIMIT_KEYS includes maxBranches for updateOverrides. But NEITHER superadmin surface exposes them:

1) PlansPage PlanModal formData (lines 294-308) builds its object WITHOUT externalDisplay, deliveryIntegration, or posAccess, and renders no checkbox for them (feature checkboxes at lines 477-567 cover only advancedReports, multiLocation, customBranding, apiAccess, prioritySupport, inventoryTracking, kdsIntegration, reservationSystem, personnelManagement). On createPlan the backend defaults these to false (createPlan: `externalDisplay ?? false`, `deliveryIntegration ?? false`), so any plan created via the SA form silently has delivery integration and partner-display OFF with no way to turn them on. posAccess isn't in createPlan/updatePlan at all (separate gap) but the per-tenant override is the intended grant path — which is also broken (below).

2) TenantDetailPage override editor: FEATURE_LABELS (lines 36-46) omits externalDisplay, deliveryIntegration, posAccess; LIMIT_LABELS (lines 48-54) omits maxBranches. featureStates is initialized only from Object.keys(FEATURE_LABELS) and buildFeatureOverridesPayload (helpers.ts:17-27) iterates only over featureStates, so those keys can NEVER be sent even though the backend whitelist accepts them. getOverrides() in superadmin-tenants.service.ts:551-565 also omits posAccess from planDefaults.features, so the editor is blind to it on read too.

User-visible symptom: A support/superadmin operator handling an escalation ('enable delivery platforms / partner display for this customer', or 'build a custom plan with delivery integration') opens the Plans form or the per-tenant Feature Overrides editor, finds NO toggle for deliveryIntegration / externalDisplay / posAccess, sets whatever IS visible, clicks Save, and gets a 'Plan kaydedildi.' / overrides-saved success toast (PlansPage.tsx:247, useUpdateTenantOverrides onSuccess at superAdminApi.ts:396) — believing the customer is now provisioned. The two highest-value gated modules are unchanged. These are real revenue gates: deliveryIntegration controls the entire delivery-platforms module (Yemeksepeti/Getir/Trendyol/Migros, shipped v3.2.39) and externalDisplay controls the Partner Display API (v3.2.32), and BOTH are advertised to customers in the plan-comparison matrix (PlanComparisonMatrix.tsx:35-36) and gate the Sidebar/SubscriptionContext. The only way to actually set them is direct DB edit / seed. Confirmed no other frontend admin surface sets these (grep of src for deliveryIntegration/externalDisplay returns only consumers: Sidebar, SubscriptionContext, PlanCard, UpgradePrompt, PartnerKeysPage, DeliveryOrdersPage).
- **suggested fix:** Expose the missing flags in both superadmin surfaces (backend already accepts them; this is purely additive FE wiring plus one backend gap):
1) PlansPage.tsx PlanModal: add externalDisplay and deliveryIntegration to formData (initialized from plan?.externalDisplay/deliveryIntegration ?? false) and add their checkboxes alongside the existing 9. Add the two fields to the superadmin SubscriptionPlan type (features/superadmin/types.ts) so they round-trip. For posAccess, either (a) add it to CreatePlanDto/UpdatePlanDto + createPlan/updatePlan (createDto.posAccess ?? <tier default>) and a checkbox, or (b) explicitly document that posAccess is per-tenant-override-only and surface it in the override editor (preferred, since the override DTO + FEATURE_KEYS already accept it).
2) TenantDetailPage.tsx: add externalDisplay, deliveryIntegration, posAccess to FEATURE_LABELS and maxBranches to LIMIT_LABELS (with i18n featureLabels/limitLabels keys). Since featureStates/limitValues are keyed off Object.keys(FEATURE_LABELS)/LIMIT_LABELS and buildFeatureOverridesPayload/buildLimitOverridesPayload iterate featureStates/limitValues, adding the labels is sufficient to make them sendable — no helper change needed.
3) superadmin-tenants.service.ts getOverrides(): add posAccess to the planFeatures object (L551-565) and maxBranches to planLimits (L567-575) so the override editor shows the correct plan-default column and effective value on read.
4) Add a regression test asserting the SA plan form keys and the override editor label sets stay in sync with backend FEATURE_KEYS/LIMIT_KEYS (mirror-drift guard, analogous to the existing plan-projector snapshot test).

## 11. [major] Menu product-level stock tracking is dead: currentStock badge/out-of-stock gate in POS never decrements on sale (deductStockForOrder has zero callers, stockTracked never set true)

- **cluster:** menu-catalog-pos
- **file:** `backend/src/modules/orders/services/orders.service.ts:1348-1403`
- **symptom:** In the POS menu grid (frontend/src/components/pos/MenuPanel.tsx:195,248-257,304-326) the product card shows a live low-stock badge (amber when currentStock<=5, red 'Out of stock'/'Tükendi' when currentStock===0) AND disables the add-to-cart card (opacity-60 cursor-not-allowed, onClick removed) when currentStock===0. An operator who enters 'Current Stock: 10' on a menu item reasonably believes the system tracks remaining inventory and will mark it out-of-stock after 10 sales. It never does — the number stays 10 forever no matter how many are sold, and a manually-zeroed item stays un-orderable forever because nothing (and no reachable UI) can change it back. The only working stock feature is the entirely separate recipe/ingredient (StockItem) subsystem.
- **detail:** OrdersService.deductStockForOrder(orderId, tenantId) is the ONLY method that decrements the Product table's currentStock and creates an OUT StockMovement for a menu product. A grep of the whole backend (excluding the *.spec) shows it has ZERO callers — it is invoked only by its own isolation spec orders.service.stock-deduction.spec.ts (lines 63, 97). Order creation (createInner), payment finalization, checkout confirm, KDS status transitions, and customer-orders all wire ONLY the recipe/ingredient path StockDeductionService.deductForOrder (orders.service.ts:624/1130, kds.service.ts:201, customer-orders.service.ts:317), which draws down stock-management StockItem rows via recipes — a completely separate subsystem. Nothing decrements Product.currentStock when an item sells. Compounding this: the menu product form (frontend/src/pages/admin/MenuManagementPage.tsx:194-200 handleProductSubmit) writes currentStock but never sends stockTracked, so ProductsService.create defaults stockTracked:false (products.service.ts:69). The frontend never sets stockTracked:true anywhere (grep of frontend src) and never calls the manual PATCH /menu/products/:id/stock endpoint — and that endpoint hard-rejects with 'Stock tracking is not enabled' when stockTracked is false (products.service.ts:266-270). So the currentStock number an operator types is write-once-decorative: it can't be auto-decremented (orphaned method), can't be manually adjusted (guard blocks it), and is never synced.
- **suggested fix:** Pick one coherent direction and wire it end-to-end:

A) If product-level stock IS a real feature: (i) add a stockTracked toggle to the menu product form (menuSchemas.ts + MenuManagementPage.tsx) and send it; (ii) call OrdersService.deductStockForOrder from the same lifecycle point as StockDeductionService.deductForOrder (orders.service.ts:624/1130, kds.service.ts:201, customer-orders.service.ts:317), ideally inside/adjacent to the same idempotent transaction with a stockDeducted-style guard so it can't double-decrement on retries/status churn; (iii) build an admin UI that calls PATCH /menu/products/:id/stock or POST /stock/movements so currentStock can be replenished/adjusted; (iv) gate the POS badge AND the cardActsAsAdd disable on product.stockTracked === true so untracked products (currentStock defaulting to 0) are never rendered out-of-stock/un-orderable.

B) If product-level stock is NOT the intended subsystem (recipe/StockItem is canonical): delete deductStockForOrder + its isolation spec, remove the unreachable /stock/movements and PATCH /menu/products/:id/stock endpoints (or document them as superadmin-only), drop the currentStock column from the menu form, and gate the POS out-of-stock badge/disable on product.stockTracked so the dead Product.currentStock value can never freeze a product as un-orderable.

Minimum stop-gap regardless of A/B: in MenuPanel.tsx make both the badge (lines 248/372) and cardActsAsAdd (lines 195/326) require product.stockTracked === true before treating currentStock===0 as out-of-stock, so the current default (untracked, currentStock 0) stops bricking add-to-cart for ordinary menu items.

## 12. [major] Per-product KDV tax rate is fixed at 10% for every menu item — Product.taxRate exists but no DTO or admin UI can set it

- **cluster:** menu-catalog-pos
- **file:** `backend/prisma/schema.prisma:390`
- **symptom:** A restaurant selling mixed-rate goods (e.g. alcohol at 20% KDV, bread/staples at 1% KDV alongside 10% food) has no way to set the correct VAT rate per item, so every line on every receipt and every z-report KDV breakdown is computed at a flat 10%. The tax totals look authoritative but are systematically wrong for any item whose real rate isn't 10%, with no operator-facing control to correct it.
- **detail:** Product.taxRate Int @default(10) carries the inline comment 'KDV rate: 0, 1, 10, 20' implying it varies per item. OrderPricingCalculator.priceItems reads product.taxRate ?? 10 (order-pricing.calculator.ts:72) to extract per-line KDV that flows into receipts, z-reports, and accounting. But taxRate is NOT present in CreateProductDto / UpdateProductDto (create-product.dto.ts has no taxRate field; UpdateProductDto = PartialType of it) and NOT in the product form or any menu/catalog admin surface (grep 'taxRate' across menu/catalog modules + frontend menu pages returns nothing). There is no superadmin or settings path either. Therefore every product is permanently locked at KDV 10%.
- **suggested fix:** Make taxRate per-product writable end-to-end: (1) Add `@ApiProperty` + `@IsInt() @IsIn([0,1,10,20]) @IsOptional() taxRate?: number;` to CreateProductDto (UpdateProductDto inherits it via PartialType). (2) In products.service.ts create() add `taxRate: createProductDto.taxRate ?? 10` to the data object, and in update() pass through taxRate when present. (3) Add a taxRate select/whitelist to the update mapping so it persists. (4) Frontend: add `taxRate: z.number().refine(v => [0,1,10,20].includes(v))` to createProductSchema, add a KDV dropdown to the product form, and add taxRate to the FE Product/CreateProductDto types. (5) Optionally backfill existing products via a one-time data migration if the tenant has known mixed-rate items, but default 10 remains safe. Add a unit/e2e test asserting a product created with taxRate=1 produces a z-report/sales-invoice line at 1%.

## 13. [major] Form validation messages render the raw key word ("email"/"minLength"/"required"/"nameMin") in ALL locales — public auth + CRM forms

- **cluster:** critic:i18n locale parity / silent raw-key rendering (ar, ru, uz, en fallback)
- **file:** `frontend/src/pages/auth/RegisterPage.tsx:30-33 (also LoginPage.tsx:48-49, ForgotPasswordPage.tsx:19, ResetPasswordPage.tsx:23, WelcomePage.tsx:48-50, CustomerFormModal.tsx:33-34)`
- **symptom:** On /login, /register, /forgot-password, /reset-password (all public routes), the post-social /welcome onboarding, and the CRM Add/Edit Customer modal: submitting an invalid email shows the literal word "email" under the field instead of "Please enter a valid email address"; a too-short password shows "minLength"; an empty required field shows "required"; a short customer name shows "nameMin". Affects every UI language, not just ar/ru/uz.
- **detail:** Every validation message in these forms is built via t('validation:email'), t('validation:minLength',{count}), t('validation:required'), t('validation:nameMin'). But the validation.json files (all 5 locales, en/tr/ru/uz/ar) nest ALL keys under a top-level "validation" object, so the real leaf keys are validation.email, validation.minLength, etc. The code passes ns=validation, key=email (i.e. expects key at the ROOT of the namespace), which does not exist. No default/fallback string is passed as the 2nd arg. With the prod i18n config (fallbackLng:'en', returnEmptyString:false, and saveMissing/missingKeyHandler DEV-only), i18next returns the bare last key segment as the rendered string. I reproduced this against the real locale JSON with the actual i18next package: t('validation:email') => "email", t('validation:minLength',{count:8}) => "minLength", t('validation:required') => "required", t('validation:nameMin') => "nameMin" — vs the correctly-nested control t('validation:validation.email') => "Lütfen geçerli bir e-posta adresi girin". This is NOT a locale-parity defect: all 5 locales are identically structured, so it fails in every language, including English. The CI i18n-parity job (scripts/check-i18n-parity.mjs, wired in quality-gates.yml:170) cannot catch it because it only diffs key-SETS across locales (and treats orphans as 'dead weight, non-blocking') — here every locale is identically wrong, so parity passes. Every t('validation:...') usage in the codebase uses this broken flat-access form; there are zero correct usages.
- **suggested fix:** Pick ONE of two consistent approaches and apply to all 13 flat call sites:

Option A (smaller code change, recommended): the JSON already nests under "validation", so just reference it. Change every t('validation:<key>') to t('validation:validation.<key>'), e.g. t('validation:validation.email'), t('validation:validation.minLength',{count:8}), t('validation:validation.required'), t('validation:validation.nameMin'). Verified to return the real localized strings.

Option B (cleaner namespace): flatten the JSON in all 5 locales — remove the wrapping top-level "validation" object so leaf keys live at the namespace root — then the existing t('validation:email') calls resolve correctly. This is more files to edit and risks other consumers, so A is safer.

In both cases also add a default/fallback 2nd arg as defense-in-depth, e.g. t('validation:validation.email', 'Please enter a valid email address'), so a future key drift degrades to readable English rather than a raw token. Files: RegisterPage.tsx:30-33, LoginPage.tsx:48-49, ForgotPasswordPage.tsx:19, ResetPasswordPage.tsx:23, src/pages/onboarding/WelcomePage.tsx:48-50, components/customers/CustomerFormModal.tsx:33-34. Consider a lint rule / test asserting t('validation:...') resolves to a non-token string, since the CI key-set parity check structurally cannot catch this class.

## 14. [major] e-Fatura/accounting sync sends ENCRYPTED credential blobs to the provider (never decrypted) → auto-sync silently always FAILS and "Test Connection" always fails even with correct creds

- **cluster:** critic:e-Fatura / yazarkasa fiscal issuance dormant-but-advertised
- **file:** `backend/src/modules/accounting/services/accounting-sync.service.ts:24, 126, 131, 161, 170-195`
- **symptom:** An ADMIN opens Settings → Accounting (AccountingSettingsPage), selects provider Parasut/Logo/Foriba, types the correct username + password/secret (saved via autosave), enables auto-sync, and clicks "Test Connection". Test always reports failure even with correct credentials; and after placing real paid orders, every e-Fatura/e-Arsiv invoice lands in FAILED (SyncStatusCard shows failed climbing, synced=0, lastSynced=Never). The tenant believes the advertised 'e-Fatura integration' is configured and live, but no invoice is ever accepted by GIB. Because maybeGenerateAutoInvoice fires the sync best-effort (errors swallowed/logged only), there is no user-facing error on the POS — turnover simply never reaches the provider.
- **detail:** WRITE path encrypts: AccountingSettingsService.update() -> encryptDto() AES-256-GCM-encrypts the four secret fields (ENCRYPTED_FIELDS = parasutClientSecret, parasutPassword, logoPassword, foribaPassword) into 'v1:iv:authTag:ciphertext' strings before persisting (accounting-settings.service.ts:17-33, 99-100). A dedicated decrypt helper getDecryptedCredentials() exists at accounting-settings.service.ts:138 (its own comment: "Read credentials in plaintext for adapter use") — but grep shows it is DEAD CODE: no non-spec runtime caller anywhere in src. READ path never decrypts: both AccountingSyncService.syncInvoice (line 24) and AccountingSyncService.testConnection (line 126) call settingsService.findByTenant(tenantId), whose real implementation (accounting-settings.service.ts:42-60) returns the raw Prisma row with the secret fields still as 'v1:' ciphertext (it does NOT decrypt). getCredentials(settings) (line 170-195) then reads settings.foribaPassword / settings.parasutPassword / settings.parasutClientSecret / settings.logoPassword directly and hands the ciphertext to adapter.authenticate() as the password/secret. The provider (ForibaEfaturaAdapter.authenticate posts the blob as grant_type=password password; foriba-efatura.adapter.ts:19-38) rejects it -> authenticate throws -> syncInvoice catch records externalStatus='FAILED'+syncError, and testConnection returns {success:false}. Username/apiUrl/clientId fields are NOT in ENCRYPTED_FIELDS so they pass through correct — the failure is specifically the secret being ciphertext. Why it slipped through: accounting-sync.service.spec.ts mocks findByTenant to return PLAINTEXT creds (e.g. foribaPassword:'p' at lines 58,259,276), so the unit tests never exercise the real encrypt-on-write/decrypt-on-read mismatch (same mocked-Prisma blind spot called out in repo memory). Reachability: AccountingSettingsController is gated @RequiresIntegration('accounting') + ADMIN, and ENCRYPTION_MASTER_KEY is set in prod (per staging/prod arch memo), so the encrypt-on-write branch IS live in production for any tenant that owns the accounting add-on and configures a provider. Fix: route both syncInvoice and testConnection credential reads through settingsService.getDecryptedCredentials(tenantId) (or decrypt inside getCredentials) instead of the raw findByTenant row. NOTE on payment-finalizer double-fiscalization guard: maybeIssueYazarkasaReceipt skips when acc.autoSync && provider!='NONE' (payment-finalizer.service.ts:630) — that guard reads the flags correctly (flags aren't encrypted), so it is unaffected; the bug is strictly the secret never being decrypted on the sync/test rails.
- **suggested fix:** Route both credential-reading rails through decryption. Either (a) change AccountingSyncService.syncInvoice (line 24) and testConnection (line 126) to call settingsService.getDecryptedCredentials(tenantId) instead of findByTenant (note: getDecryptedCredentials currently returns null when no row exists, so guard for null and treat provider as NONE), or (b) decrypt inside getCredentials by running decryptString on the four ENCRYPTED_FIELDS before returning. Option (a) is cleaner since the helper already exists and tolerates legacy plaintext via the v1: prefix check. Then fix the test blind spot: in accounting-sync.service.spec.ts have findByTenant/getDecryptedCredentials return creds that exercise the encrypt-then-decrypt round-trip (or add a real-DB e2e per the repo's existing backend-e2e gate) so the mismatch can't regress. getCompanyId is unaffected (companyId fields aren't encrypted).

## 15. [minor] reject/cancel/cancelPublic fire customer notification as an uncaught floating promise — send failures silently swallowed (no Sentry), unlike created/confirmed

- **cluster:** reservations
- **file:** `backend/src/modules/reservations/services/reservations.service.ts:750, 929, 1031`
- **symptom:** An operator rejects or cancels a reservation and sees a success toast, but if the notification send fails the customer is never told and the failure is invisible to operators/monitoring (no error surfaced anywhere), unlike the created/confirmed events which are captured to Sentry on failure.
- **detail:** created/confirmed route through notifyCustomer() which wraps notify() in `void ...catch(captureException)` (lines 61-77) precisely because the audit (comment lines 56-60) flagged unawaited notify() calls as dropping send failures. But reject (line 750), cancel (line 929) and cancelPublic (line 1031) call this.reservationNotificationService.notify(...) directly — not awaited and not .catch()-wrapped. If the underlying email/SMS send rejects (provider throw), the rejection is dropped: no Sentry capture, no log at the reservations layer, and an unhandled promise rejection on the Node process. The admin/customer still gets the 'rejected'/'cancelled' success toast.
- **suggested fix:** Route reject/cancel/cancelPublic through the existing notifyCustomer() wrapper so failures get the same structured Sentry capture and (for parity) the metric counter. Two steps:

1. Widen notifyCustomer's event union and the captureException context to include "rejected" | "cancelled":
   private notifyCustomer(tenantId: string, event: "created"|"confirmed"|"rejected"|"cancelled", payload: ...): void { ... }
   (countReservation already accepts any string; optionally update its doc-string label.)

2. Replace the three bare calls. E.g. at line 750:
   this.notifyCustomer(scope.tenantId, "rejected", { customerName, customerEmail, customerPhone, date: rejectDateStr, startTime, reservationNumber, reason: rejectionReason });
   line 929: this.notifyCustomer(scope.tenantId, "cancelled", { ... });
   line 1031: this.notifyCustomer(tenantId, "cancelled", { ... });

This collapses all 5 lifecycle events onto one fire-and-forget path with .catch(captureException) carrying {module, op, event, tenantId}, and removes the chance of a context-less global-handler capture. Then extend reservations.observability.spec.ts with a rejected/cancelled rejection case asserting mockCapture is called with the matching event, mirroring the existing confirmed test. (Note: since the real rejection vector is the unguarded findByTenant DB read in notify(), this fix is about consistent observability, not preventing a frequent failure.)

## 16. [minor] QR-menu loyalty transaction history fetched from a non-existent path — history list is permanently empty

- **cluster:** loyalty-customers
- **file:** `frontend/src/pages/qr-menu/SubdomainLoyaltyPage.tsx:30-31`
- **symptom:** On the customer Rewards page, the 'Transaction History' accordion never appears (it is gated on transactions.length > 0). A customer who has earned/redeemed points sees no history at all, with no error — it just looks like they have no transactions.
- **detail:** The corresponding tests assert the buggy URL (LoyaltyPage.test.tsx, SubdomainLoyaltyPage.test.tsx both expect '/sessions/<id>/loyalty/transactions'), so the broken contract is locked in by the test suite. Fix: GET customer-public/loyalty/transactions?sessionId=<id> and update the two test expectations.
- **suggested fix:** Change both fetches to the real backend route (query-param style), matching the sibling LoyaltyDisplay call. In frontend/src/pages/qr-menu/SubdomainLoyaltyPage.tsx:30-31 and frontend/src/pages/qr-menu/LoyaltyPage.tsx:28-29 replace the URL with:
`${API_URL}/customer-public/loyalty/transactions?sessionId=${sessionId}`
Then update the two test expectations (LoyaltyPage.test.tsx:65 and SubdomainLoyaltyPage.test.tsx:54) to assert the corrected query-param URL. Optionally guard against the no-customer case (the backend throws 400 when the session has no customerId) so the empty-history-before-identify state stays silent. Note: LoyaltyContent.tsx:57 has a similar mismatch (`POST customer-public/loyalty/apply-referral` with a body `customerId`, while the backend referral route is `POST referral/apply` taking sessionId) — worth flagging separately but out of scope for this transactions fix.

## 17. [minor] Referral program card is shown to every QR customer but no UI path ever generates a referral code

- **cluster:** loyalty-customers
- **file:** `frontend/src/components/qr-menu/LoyaltyDisplay.tsx:59-63`
- **symptom:** The 'Referral Program / Share & Earn' card always shows the 'noReferralCode' empty state for a customer — they can never get a code to share. The copy-code button and stats are effectively dead because referralData.referralCode is always empty.
- **detail:** The backend generate endpoint works; the UI just never calls it. Combined with finding #1 (apply is also broken), the entire customer-facing referral loop is non-functional from the QR menu despite a fully rendered, polished referral UI. Fix: call POST customer-public/referral/generate { sessionId } (e.g. on first view when no code) so stats has a code to display.
- **suggested fix:** Wire the QR-menu loyalty flow to mint a code. Easiest: in LoyaltyDisplay.fetchLoyaltyData, after the stats fetch, if the customer is identified (loyaltyData.identified) and referralData.referralCode is empty, POST customer-public/referral/generate { sessionId } and re-read stats (the endpoint is idempotent — generateReferralCode returns the existing code if one is already set, referral.service.ts:50). Alternatively, generate the code at identify-time (customer-public.controller.ts identifyCustomer, alongside awardWelcomeBonus) so stats always has a code to show. If the 'contact support'/admin-issued model is the intended design, add a generate button to the admin CustomerDetailPage (it currently only displays the code) so support actually has a way to mint one; otherwise the referrer side of the program is permanently inert.

## 18. [minor] Analytics "Customer Behavior" tab and Overview "Congestion Score" present CV-camera metrics that are silently zero/default for tenants without camera hardware, with no "requires cameras" disclosure

- **cluster:** reports-exports
- **file:** `frontend/src/pages/admin/AnalyticsPage.tsx:279, 611, 617, 623`
- **symptom:** On /admin/analytics a paying tenant with no CV edge cameras sees: Overview/Traffic "Congestion Score 100/100" with "0 hotspots" (looks like a perfect, working metric — actually "no data"); and the "Customer Behavior" tab renders "Avg Idle Time 0 min", "Avg Party Size 2.5" (a hardcoded fallback), and peak arrival/departure hours derived from order timestamps only. The user reasonably believes these are measured behavioral analytics, but party size (2.5) is a literal constant fallback and idle time is structurally always 0 because no occupancy telemetry exists.
- **detail:** getCustomerBehavior (backend/src/modules/analytics/services/table-analytics.service.ts:260-383) reads avgIdleDuration from tableAnalytics — which the honest real producer always writes as null/0 (table-analytics-producer.service.ts:160-167, 'Idle requires occupancy telemetry to measure — honestly 0 here'). avgPartySize is computed from occupancyRecord rows with state:'SITTING' (line 337-350), which only the CV edge-device gateway produces; with no cameras, partyCount is 0 and the method returns the literal fallback `2.5` (line 371). Congestion (heatmap.service.getCongestionAnalysis) reads trafficFlowRecord (CV-only) and the controller/UI default overallScore to 100 when empty (AnalyticsPage.tsx:279,505). The underlying data path is legitimate hardware-gated CV ingestion (analytics.gateway.ts handleOccupancyData is a real producer), so this is NOT fabricated data — but the UI gives no signal that these specific cards require camera hardware, so a camera-less paying tenant cannot distinguish 'measured zero' from 'no sensor'. Lower-confidence than candidate 1; flagging because it is reachable by any advancedReports tenant and the 2.5/100 defaults read as real measurements.
- **suggested fix:** Add a hardware-source disclosure for the CV-derived cards rather than rendering misleading defaults: (1) For Congestion Score, when congestion?.congestionPoints is empty/undefined show an em-dash or "No camera data" instead of `|| 100`, and do not color it green-healthy (AnalyticsPage.tsx:279,282,505,508). (2) For Avg Party Size, return null from the backend when partyCount===0 (drop the 2.5 literal at table-analytics.service.ts:371) and render "—" with a "Requires occupancy cameras" subtitle (AnalyticsPage.tsx:622-627). (3) For Avg Idle Time, surface a "Requires occupancy cameras" caveat since it is structurally 0 without telemetry (AnalyticsPage.tsx:615-621). Optionally drive these by a tenant cameras-present flag (CameraService) so camera-equipped tenants see real values and camera-less tenants see the disclosure. Also replace the dev-oriented "Generate mock data to see analytics" empty-state copy (line 699) with a production-appropriate message.

## 19. [minor] Real-time low-stock alert on order auto-deduction emits to a topic no client subscribes to (orphaned socket event)

- **cluster:** stock-inventory
- **file:** `backend/src/modules/kds/kds.gateway.ts:908-916`
- **symptom:** Kitchen/POS operators never get a real-time toast when a completed order draws an ingredient below its minimum stock level, even though the feature is fully built server-side and the backend logs that the alert was sent. They silently rely on the once-per-hour batch alert instead, so an item can be oversold/run dry between hourly ticks with no in-the-moment warning.
- **detail:** StockDeductionService.deductForOrder returns a real lowStockAlerts[] array whenever an order's auto-deduction drops an ingredient at/below its minStock (stock-deduction.service.ts lines 287-292 push the item name onto lowStockAlerts; line 114 returns it). All four order-completion call sites consume that array and, when non-empty, call this.kdsGateway.emitLowStockAlert(...): orders.service.ts:632 (order create, PENDING) and :1134 (status change), kds.service.ts:206 (KDS status set), customer-orders.service.ts:317 (PENDING_APPROVAL). emitLowStockAlert (kds.gateway.ts:908) emits the event name "stock:low-stock-alert" to the kitchen-/pos- rooms and even logs `stock:low-stock-alert N items → kitchen/pos` (line 913-915) implying delivery. BUT the frontend KDS and POS sockets only subscribe to "stock:low-alert" and "stock:expiry-alert" (frontend/src/features/kds/useKitchenSocket.ts:126-127 and frontend/src/features/pos/usePosSocket.ts:512-513) — the events emitted by the SEPARATE method emitStockLowAlert (kds.gateway.ts:925, scheduler/dashboard path). A repo-wide grep finds ZERO runtime consumers of "stock:low-stock-alert" anywhere (frontend, backend, or native); the only other reference is docs/e2e-test-plan.md which marks 'socket emit on stock < threshold' as ❌ not-done. The gateway's own comment at lines 918-924 documents that clients subscribe to "stock:low-alert"/"stock:expiry-alert" and pointedly omits "stock:low-stock-alert" — the orphaned event directly above it. NET EFFECT: the in-the-moment, sales-driven 'this order just pushed flour below minimum' warning that this whole code path is built to deliver is silently dropped (Socket.IO emit to an event with no listeners is a no-op). Operators only ever see the hourly StockAlertsScheduler batch alert (stock:low-alert), never the immediate per-order one. The backend wires it, returns the data, calls the emit, and logs success — but nothing renders. Fix is a one-line topic alignment: emit "stock:low-alert" from emitLowStockAlert (and adapt the payload shape to {count, items}), or add a "stock:low-stock-alert" handler in the two socket hooks.
- **suggested fix:** One-line topic alignment in kds.gateway.ts:908-916. Change emitLowStockAlert to emit "stock:low-alert" (the topic the KDS/POS hooks already subscribe to) AND fix the payload shape to include count, since the FE handlers read event.count: `.emit("stock:low-alert", { count: items.length, items, timestamp: new Date() })`. Update the log string accordingly. This reuses the existing rendered handler (handleStockLowAlert) so per-order alerts surface the same warning toast as the hourly batch. Alternatively (more code, not recommended) add a dedicated `socket.on('stock:low-stock-alert', ...)` handler in both useKitchenSocket.ts and usePosSocket.ts reading event.items. Prefer the emit-side fix. Note the i18n key kitchen:kitchen.lowStockAlert interpolates {count}, so passing count is required for a non-zero message.

## 20. [minor] Backend onboarding/tour persistence (GET/PATCH /users/me/onboarding + user.onboardingData) is dead — no client ever calls it; onboarding state is localStorage-only, so welcome/tour re-trigger on every new device/browser

- **cluster:** onboarding-demo
- **file:** `backend/src/modules/users/services/user-onboarding.service.ts:22-69`
- **symptom:** Welcome modal and guided tour reappear (and 'skip all tours' is forgotten) whenever the user opens the app in a different browser/device, in incognito, or after clearing browser data — onboarding progress does not follow the account even though a server-side store for it exists and is exposed.
- **detail:** The backend exposes a complete, guarded, reachable per-user onboarding store: UsersController GET `me/onboarding` and PATCH `me/onboarding` (backend/src/modules/users/users.controller.ts:188-203) delegate to UserOnboardingService.getOnboarding/updateOnboarding (user-onboarding.service.ts:22-69), which read/write the `user.onboardingData` JSON column (hasSeenWelcome, tourProgress, skipAllTours). This exists precisely to make onboarding progress survive device/browser changes (it's keyed per user, server-side).

BUT NO CLIENT ANYWHERE CALLS IT. Exhaustive repo grep for `me/onboarding` and any api.get/post/patch touching `onboarding` returns zero hits outside the controller itself (no frontend, no native, no tests, no docs). The only thing that touches `onboardingData` in the entire repo is this service. Confirmed: `rg -rn "onboardingData" --include=*.ts --include=*.tsx .` => only user-onboarding.service.*; `grep -rn "me/onboarding"` outside the controller => empty.

Instead, ALL onboarding/tour state lives purely in the browser: frontend/src/store/uiStore.ts wraps `onboarding` (hasSeenWelcome, tourProgress, skipAllTours) in zustand `persist` with name `ui-storage` (localStorage, partialize at uiStore.ts:162-168). useOnboarding.ts computes `shouldShowWelcome = !!user && !onboarding.hasSeenWelcome && !onboarding.skipAllTours && !hasCompletedTour` (useOnboarding.ts:74-78) and writes progress via setHasSeenWelcome / updateTourProgress / setSkipAllTours — all uiStore (localStorage) only, never POSTed to the backend.

USER-VISIBLE SYMPTOM: A user who completes (or 'skip all tours' on) the welcome flow on one browser/device sees the full WelcomeModal + guided-tour beacon AGAIN on a different browser, a different device, in incognito, or after clearing site data — because the dismissal was only saved to that one browser's localStorage. The server-side store built to remember this is silently never used. It is not a crash or money bug, but it is reachable in production for every account and an operator would reasonably believe 'I already dismissed/finished the intro' persists with their account. The backend endpoints also advertise (via Swagger ApiOperation 'Get/Update current user onboarding data') a persistence contract that no part of the product fulfills.
- **suggested fix:** Wire the existing endpoints to the client so onboarding state follows the account. Concretely: (a) Add an onboarding API client (e.g. frontend/src/features/onboarding/onboardingApi.ts) calling GET /users/me/onboarding (treat as tenant-wide; the route is @SkipBranchScope) and PATCH /users/me/onboarding. (b) On app boot / after auth, hydrate uiStore.onboarding from the server response (merging server-truth over the localStorage cache so a fresh browser inherits the account's dismissed/completed state) — gate shouldShowWelcome on the hydrated value so the modal doesn't flash before the fetch resolves. (c) In useOnboarding.ts, make closeWelcomeModal/startTour/skipTour and updateTourProgress also PATCH the server (debounced/fire-and-forget) in addition to writing uiStore, so hasSeenWelcome/skipAllTours/tourProgress persist per user. Keep localStorage as an offline cache but server as source of truth. Minimal viable version: just persist hasSeenWelcome + skipAllTours (the dismissal bits) to fix the core re-trigger symptom; tourProgress sync is optional. If product instead decides onboarding is deliberately per-device, the honest alternative is to delete the dead service/controller endpoints + the onboardingData column and drop the misleading Swagger contract.

## 21. [minor] Orphaned product images grow monotonically: nothing prunes ProductImage rows/files on product-delete or image-detach, and the 'unused images' cleanup capability is wired backend-to-hook but has NO UI consumer (dead end)

- **cluster:** critic:file upload → static serving round-trip + orphaned product images
- **file:** `backend/src/modules/upload/upload.service.ts:348-377 (getUnusedImages); 267-307 (deleteProductImage); products.service.ts:223-260 (update/remove); products.service.ts:436-471 (removeImageFromProduct)`
- **symptom:** Operator deletes products or removes/replaces a product photo expecting storage to be freed; instead orphaned image rows + .jpg files accumulate indefinitely (DB table + persisted uploads volume grow monotonically). The 'unused images' backend endpoint + frontend hook that would let them find and clear the junk are implemented but never surfaced in any screen or cleanup job, so there is no way to actually reclaim it.
- **detail:** CHECK (2) confirmed as a real looks-working-but-isnt defect. The only code path that ever unlinks a product-image file AND deletes its DB row is UploadService.deleteProductImage (upload.service.ts:267), reachable solely via the manual trash button in the Images library tab (frontend ImagesTab.tsx:66-79, one image at a time). Every OTHER path orphans images:

1. PRODUCT HARD-DELETE (products.service.ts:239 remove): prisma.product.delete cascades only the ProductToImage junction rows (schema.prisma:443 onDelete:Cascade). ProductImage has NO direct FK to Product (only the junction at schema.prisma:431/446), so the ProductImage rows AND their on-disk .jpg files SURVIVE the product deletion forever.
2. PRODUCT UPDATE with imageIds (products.service.ts:223-227): replaces links via productToImage.deleteMany — previously-attached images become orphans (row + file remain).
3. DETACH (products.service.ts:464 removeImageFromProduct): deletes only the junction row; the comment literally says 'image stays in library for reuse'. File never unlinked, row never removed.

The codebase DOES have the exact tool to surface this accumulation — getUnusedImages (upload.service.ts:348) finds ProductImage rows not in any junction link — exposed at GET /upload/product-images/unused (upload.controller.ts:227-240) and even given a frontend hook useUnusedImages (frontend/src/features/upload/uploadApi.ts:80-88). BUT: grep across all of frontend/src shows useUnusedImages appears ONLY in its own definition (uploadApi.ts) and uploadApi.test.tsx — ZERO rendering components import it. The actual Images library UI (ImagesTab.tsx:48) calls useProductImages (ALL images), not the unused one. There is ALSO no @Cron / scheduled cleanup job anywhere in backend/src (grep for @Cron/orphan/prune/unused over the backend found nothing image-related). The uploads dir is a persisted docker volume (docker-compose.prod.yml:131 uploads_storage:/app/uploads), so files never get reclaimed by container churn either.

NET RESULT: with normal use (editing a product's images, deleting products, detaching images) both the product_images table and the /app/uploads/products/<tenantId>/ directory grow without bound, and the one feature an operator could use to find+reclaim the junk is reachable only by typing the raw URL — there is no button, page, or job that calls it. So an operator who assumes 'deleting a product / removing its photo frees the storage' is wrong, and the 'unused images' capability that exists in the code is effectively a dead end.

NOTE (NOT a separate finding): logo uploads DO self-prune via pruneTenantLogos (upload.service.ts:124/144) so the logo category is bounded; only product images leak. NOTE on severity: this is operational/storage-bloat, not a user-visible error — images still render fine; the user-visible aspect is the missing/unreachable cleanup affordance.
- **suggested fix:** Two-part fix. (1) Surface the existing unused-images capability: add a tab/filter toggle in ImagesTab.tsx that calls the already-implemented useUnusedImages hook (frontend/src/features/upload/uploadApi.ts:80) so operators can see orphans and bulk-delete them via the existing handleDeleteSelectedImages path — this turns the dead-end endpoint into a working reclaim screen with almost no new backend code. (2) Auto-prune on the orphaning paths in backend/src/modules/menu/services/products.service.ts: after productToImage.deleteMany in update() (line 225) and after the junction delete in removeImageFromProduct() (line 464), and on product.delete in remove() (line 244), detect ProductImage rows for this tenant that are now linked to no product and delete them via UploadService.deleteProductImage (which already unlinks the file + row safely with the uploadsRoot path guard). Prefer event-driven (emit a product/image-detached event consumed by the upload module) or a daily @Cron sweep that runs getUnusedImages per tenant and deletes rows older than a grace window (e.g. 24h) — mirroring the existing pruneTenantLogos best-effort pattern (upload.service.ts:144) and the other tenant-addon-sweeper @Cron at backend/src/modules/marketplace/tenant-addon-sweeper.service.ts:51. A grace window avoids deleting an image a user just uploaded but hasn't attached yet.

## 22. [minor] Attendance overtime/worked-minutes tracked + displayed but never costed or exportable — payroll claim is unbacked (no wage field, no payroll export anywhere)

- **cluster:** critic:personnel attendance overtime → payroll dead-end
- **file:** `backend/src/modules/personnel/services/attendance.service.ts:228-243 (clockOut computes totalWorkedMinutes+overtimeMinutes); 470-519 (getAttendanceSummary aggregates them, display-only)`
- **symptom:** A manager sees per-employee 'Total Overtime'/'Overtime: 3h' and 'Total Hours' in the Personnel > Attendance screen and the clock-in note flow is documented as feeding 'payroll export', creating the reasonable belief that hours/overtime can be exported or are used to compute wages — but there is no wage rate stored, no labor-cost calculation, and no payroll export anywhere, so the numbers can never produce a payroll/pay output. Severity is moderate: the data is real and accurate, but the implied payroll capability does not exist (and the DTO doc claim is outright false).
- **detail:** clockOut() computes and persists totalWorkedMinutes and overtimeMinutes (attendance.service.ts:228-243), and getAttendanceSummary() aggregates totalWorkedMinutes/totalOvertimeMinutes per user (470-519). These flow ONLY to read-only UI tables: AttendanceTab.tsx renders Worked Hours + an 'Overtime' column (frontend/src/components/personnel/AttendanceTab.tsx:266-267, 286-287 for history; 325-326, 343-344 for summary). A repo-wide grep confirms there is NO wage / hourlyRate / salary / payRate / laborCost / compensation field ANYWHERE in prisma/schema.prisma (Attendance model lines 3179-3209 has no monetary field) and NO payroll/labor-cost export or report endpoint anywhere: backend/src/modules/reports/reports.controller.ts exposes sales/top-products/payments/orders-by-hour/customers/inventory/staff-performance only; staff-performance (reports.service.ts ~618-665) and PerformanceService (performance.service.ts:43-152) consume totalWorkedMinutes ONLY to derive ordersPerHour productivity, never pay. The personnel controllers expose no 'payroll' or 'export' route, and the personnel frontend (AttendanceTab) has no Export/Download/Payroll button. The most concrete defect: backend/src/modules/personnel/dto/clock-in.dto.ts:5-7 documents that the `notes` field 'appears on payroll export and labor audit trail' — neither a payroll export NOR a labor audit trail exists in the codebase; `notes` is not even rendered as a column in AttendanceTab. So overtime is shown to operators ('overtime: 3h') as if it feeds compensation, but it is never converted to cost and cannot be exported for an actual payroll run.
- **suggested fix:** Two-part honesty fix. (1) Immediate, zero-risk: correct the false contract claim in backend/src/modules/personnel/dto/clock-in.dto.ts:5-7 — change the comment to describe what notes actually do (persisted to Attendance.notes, shown in attendance detail/audit context) and drop the 'payroll export and labor audit trail' assertion, OR actually surface notes somewhere (it is currently not even a column in AttendanceTab.tsx). (2) Decide product intent for overtime: either (a) relabel the UI so overtime/worked-hours are clearly attendance/scheduling metrics (not implied compensation inputs), or (b) if payroll is intended, build the missing rail end-to-end — add an hourlyRate/wage field (on User or a new PayRate model), a labor-cost computation that multiplies worked+overtime minutes by rate (with an overtime multiplier), and a payroll/labor-cost export endpoint + an Export control in AttendanceTab. Until (b) exists, do not document or imply payroll capability anywhere.

