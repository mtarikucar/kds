# Base URL of the marketing backend API, INCLUDING the /api prefix
# (same convention as the source repo's lib/env.ts). The marketing backend
# conventionally runs on 3100 — core's backend owns 3000.
VITE_API_URL=http://localhost:3100/api
